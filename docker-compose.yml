version: '3'
services:

  db-recipes:
    container_name: ${POSTGRES_HOST}
    image: postgres:16
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
#  groceries-api:
#    build:
#      context: ./groceries-api
#      dockerfile: /Dockerfile
#    ports:
#      - "8080:8080"
#    environment:
#      POSTGRES_HOST: ${POSTGRES_HOST}
#      POSTGRES_DB: ${POSTGRES_DB}
#      POSTGRES_USER: ${POSTGRES_USER}
#      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#    depends_on:
#      - db-recipes